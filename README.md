# Работа с массивами в C

## 📋 Обзор лабораторных работ

Проект содержит реализацию функций для работы с одномерными массивами в языке C, включая лабораторные работы 11, 14 и 16.

## 📋 Задания к лабораторным работам

### Лабораторная работа 11
- Заполнение массива с консоли
- Преобразование массива
- Печать массива

### Лабораторная работа 14
- Заполнение массива значениями из функции `Func(x)`
- Печать массива
- Вычисление суммы элементов массива
- Поиск в массиве значения равного a

### Лабораторная работа 16
- Работа с динамическими массивами
- Обработка математических функций
- Генерация случайных чисел
- Удаление и вставка элементов

## 🔧 Реализованные функции

### 1. `double* full_elements(double *ptr_array, int size)`
**Назначение**: Заполнение массива значениями функции `f(x) = 5x³ + 2x² - 15x - 6`  
**Параметры**:
- `ptr_array` - указатель на массив
- `size` - размер массива  
**Возвращает**: указатель на заполненный массив

### 2. `int put_elements(double *ptr_array, int size)`
**Назначение**: Вывод элементов массива на экран  
**Параметры**:
- `ptr_array` - указатель на массив
- `size` - размер массива  
**Возвращает**: 0 при успешном выполнении

### 3. `double* calc_elements(double *ptr_array, int size)`
**Назначение**: Округление всех элементов массива вверх с помощью `ceilf()`  
**Параметры**:
- `ptr_array` - указатель на массив
- `size` - размер массива  
**Возвращает**: указатель на преобразованный массив

### 4. `double* fill_random(double *ptr_array, int size)`
**Назначение**: Заполнение массива случайными числами в диапазоне [-1.00, 1.00]  
**Параметры**:
- `ptr_array` - указатель на массив
- `size` - размер массива  
**Возвращает**: указатель на заполненный массив

### 5. `int delete_k(double *ptr_arr, int size, double element)`
**Назначение**: Удаление элементов, значение которых превышает заданное `element`  
**Параметры**:
- `ptr_arr` - указатель на массив
- `size` - исходный размер массива
- `element` - пороговое значение  
**Возвращает**: новый размер массива после удаления

### 6. `int find_last_negative(double *ptr_arr, int size)`
**Назначение**: Поиск индекса последнего отрицательного элемента в массиве  
**Параметры**:
- `ptr_arr` - указатель на массив
- `size` - размер массива  
**Возвращает**: индекс последнего отрицательного элемента или -1, если таких элементов нет

### 7. `double* insert(double *ptr_arr, int* size, int index, double num)`
**Назначение**: Вставка элемента `num` в массив на позицию `index`  
**Параметры**:
- `ptr_arr` - указатель на массив
- `size` - указатель на размер массива
- `index` - позиция для вставки
- `num` - значение для вставки  
**Возвращает**: указатель на массив после вставки

## 📁 Исходный код

### Файл `array.h`
```c
#ifndef ARRAYS_H
#define ARRAYS_H

#include <stdio.h>
#include <stdlib.h>
#include <math.h>

double* full_elements(double* ptr_array, int size);
int put_elements(double* ptr_array, int size);
double* calc_elements(double* ptr_array, int size);
double* fill_random(double* ptr_array, int size);
int delete_k(double* ptr_arr, int size, double element);
int find_last_negative(double* ptr_arr, int size);
double* insert(double* ptr_arr, int* size, int index, double num);

#endif
```

### Файл `main.c`
```c
#define _CRT_SECURE_NO_DEPRECATE
#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
#include <math.h>
#include <time.h>
#include "array.h"

int main() {
    setlocale(LC_ALL, "");
    srand(time(NULL));

    // ... реализация основной программы
    return 0;
}
```

## 📊 Примеры работы

### Пример 1: Работа с математической функцией
```
Введите размер массива: 5
Начальный массив: 
a[5] = -13.33 -12.44 -11.41 -10.24 -8.93
Массив после изменения: 
a[5] = -13.00 -12.00 -11.00 -10.00 -8.00
```

### Пример 2: Работа со случайными числами
```
ЛР 16: часть 2
Введите размер массива для задания 2: 6
Массив со случайными числами от -1 до 1:
a[6] = 0.12 -0.45 0.89 -0.23 0.56 -0.91

2.1 Удаление элементов > A 
Введите значение A: 0.5
Удалено элементов: 2
Массив после удаления элементов:
a[4] = 0.12 -0.45 -0.23 -0.91

2.2 Вставка -999 после последнего отрицательного
Массив после вставки -999:
a[5] = 0.12 -0.45 -0.23 -0.91 -999.00
```

## Информация о разработчике

**Маркина Елена**, группа бТИИ-251
